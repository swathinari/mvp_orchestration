CREATE TABLE routing_policies(id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,tenant_id varchar(80) NOT NULL,policy_name varchar(80) NOT NULL,channel varchar(30) NOT NULL,provider_ids varchar(500) NOT NULL,max_attempts integer NOT NULL,UNIQUE(tenant_id,policy_name,channel));
